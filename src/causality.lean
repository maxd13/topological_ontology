import ontology
universe u
open set topological_space classical
local attribute [instance] prop_decidable


namespace ontology

variables {ω : ontology} (w : ω.world) (e₁ e₂ : ω.entity)

def counterfactual_dependence : Prop := sorry

end ontology